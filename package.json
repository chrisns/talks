{
  "name": "talks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --check .",
    "build": "marp && \\cp -vr images dist/"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "-": "^0.0.1",
    "@marp-team/marp-cli": "^1.5.2"
  },
  "devDependencies": {
    "prettier": "2.5.1"
  }
}
