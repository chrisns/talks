{
  "name": "talks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --check .",
    "build": "postcss themes --ext css -d themes --no-map && marp && cp -vr images dist/",
    "start": "run-p postcss-watch marp-server",
    "postcss": "postcss --ext css -d themes themes",
    "postcss-watch": "postcss --watch --ext css -d themes themes",
    "marp-server": "marp -s"
  },
  "author": "Chris Nesbitt-Smith",
  "license": "MIT",
  "postcss": {
    "map": false
  },
  "dependencies": {
    "@marp-team/marp-cli": "^1.5.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^9.1.0",
    "prettier": "2.5.1"
  }
}
